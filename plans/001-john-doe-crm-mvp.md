# 001 — John Doe CRM MVP build

> Generated by **app-forge** from `H:\AppTracker Docs\john-doe-crm\` (spec v1.0 + architecture diagram).
> Scaffold + data model are DONE (see Status). This plan covers the feature build, which needs the DB.
> Execute with `superpowers:subagent-driven-development`. Obey this repo's `AGENTS.md`.

**Goal:** the accepted MVP (spec §9): capture a lead → work to won → auto-create client → open a
project → raise & send an invoice → add a dictated voice note to any of them → see it all on the
dashboard — behind login, on Neon, deployable to Vercel.

## Status (done before this plan, no DB needed)
- ✅ Trunk cloned to `H:\john-doe-crm`, renamed to **John Doe CRM** / `john-doe-crm`, origin detached.
- ✅ `npm install`; `.env.local` written (BETTER_AUTH_SECRET generated, OWNER_EMAIL set, **DATABASE_URL is a placeholder**).
- ✅ **Data model**: 7 CRM tables + 6 enums appended to `src/lib/db/schema.ts` (additive — Better-Auth/notes untouched). Migration `drizzle/migrations/0003_common_hemingway.sql` generated. `typecheck` clean.

## Blocked on DB (do first when a Neon DB is available)
- [ ] **T-DB:** put a real Neon `DATABASE_URL` in `.env.local` (free a Neon project / upgrade / `vercel marketplace add neon`), then `npm run db:migrate`. Everything below assumes this is done so you can run + verify.

---

## Reference patterns (this trunk) — follow them, don't reinvent
- **CRUD + UI:** `src/app/(app)/notes/` (list / `[id]` / `new` / `_components`) + its server actions + `src/lib/db/queries.ts`. Mirror this shape for each entity.
- **Role gate:** `src/lib/auth/roles.ts` (`getSession`/`requireSession`/`requireRole`/`isOwner` + `OWNER_EMAIL` bypass) and the `(app)/admin/` module.
- **Dictation (port):** AppTracker (`h:\apptracker`) ships the exact feature the spec wants — `useDictation` hook + `MicButton` (Web Speech default + optional Whisper server route, last-engine persistence). Copy those primitives over; this trunk has no dictation module.
- **Money:** `numeric(12,2)` columns return **strings** in Drizzle — add a `formatMoney(s: string)` util; never do float math on them.
- **Env:** any new key → `src/lib/env.ts` zod schema **and** `.env.example` (AGENTS.md rule).

---

### T0 — Role model (spec §2): owner / rep / viewer
Spec wins over the trunk's `user|admin`. Touches existing code → do first, verify by signing in.
- [ ] In `src/lib/db/schema.ts` change `user.role` default `"user"` → `"viewer"`; `npm run db:generate` (new migration) + `db:migrate`.
- [ ] Rewrite `src/lib/auth/roles.ts`: `export type Role = "owner" | "rep" | "viewer"`; rank `{owner:3, rep:2, viewer:1}`; `effectiveRole` returns `"owner"` when `isOwner(email)`; `requireRole(min)` passes when `rank(effective) >= rank(min)`; `hasRole(session, min)` helper for UI.
- [ ] Update the `(app)/admin/` module (user list + role editor): gate on `requireRole("owner")`; role-editor options = `rep | viewer` (owner is the OWNER_EMAIL user, not assignable). This is the spec's "invite/manage users" (owner-only).
- [ ] Verify: `typecheck` + sign in as OWNER_EMAIL → full access; a second user defaults to viewer (read-only).

### T1 — Leads (entry of the lifecycle) — spec §3.1, §5
- [ ] `src/lib/db/queries/leads.ts`: list (filter by status/owner, order by `lastActivityAt`), get, create, update, updateStatus, setScore. Compute a `goingCold` flag (`lastActivityAt` > 14 days) — spec §5.
- [ ] `src/app/(app)/leads/actions.ts` (server actions, `requireRole("rep")` for writes): create/update/setStatus/setScore + `convertToClient` (see T2).
- [ ] Pages: `leads/page.tsx` (table: name, company, status badge, score, value, going-cold dot), `leads/new/page.tsx` (form: name, company, email, phone, source, est_value), `leads/[id]/page.tsx` (detail + status control + score + activity log + "Convert to client" when `qualified`/`won`).
- [ ] Verify: create a lead, move new→contacted→qualified→won, set score.

### T2 — Clients (auto-created from a won lead) — spec §3.2
- [ ] `convertToClient(leadId)` in leads actions: insert `clients` (copy name/company, set `leadId`, `ownerId`), set lead `status="won"`, log a `stage_change` activity, redirect to the client. History carries over because activities are queried by subject.
- [ ] `src/lib/db/queries/clients.ts` + `clients/page.tsx` (list) + `clients/[id]/page.tsx` (contact + billing + projects list + invoices list + activity log).
- [ ] Verify: closing a lead as won creates the linked client.

### T3 — Projects (under a client) — spec §3.3
- [ ] `src/lib/db/queries/projects.ts` + `projects/actions.ts` (create/update/setStatus, `requireRole("rep")`).
- [ ] `clients/[id]` gets an "Add project" form + project list; `projects/[id]/page.tsx` (title, description, status proposed→active→on_hold→delivered, start/due dates, fee).
- [ ] Verify: open a project on a client.

### T4 — Invoices + lines + PDF — spec §3.4, §8
- [ ] `src/lib/db/queries/invoices.ts` + `invoices/actions.ts`: create (against client, optional project), add/remove line, recompute subtotal/tax/total from lines, `send` (status draft→sent), `markPaid`/`markOverdue`. Invoice-delete restricted to owner (spec §2: reps can't delete invoices).
- [ ] `invoices/page.tsx` (list + status), `invoices/[id]/page.tsx` (lines editor + totals + status actions).
- [ ] PDF: `src/app/api/invoices/[id]/pdf/route.ts` using `jspdf` (add dep + `src/lib/env.ts` if any). MVP exports a PDF; **no card payments** (spec §8 — stub a disabled "Pay online" with a POST-MVP note).
- [ ] Verify: raise an invoice, add lines, send, download the PDF.

### T5 — Activities log + "going cold" — spec §5
- [ ] `src/lib/db/queries/activities.ts`: list by `(subjectType, subjectId)` desc; create (kind, body). On any activity insert, bump the parent lead's `lastActivityAt`.
- [ ] `<ActivityLog subjectType subjectId />` component reused on lead/client/project detail: timeline + "Log activity" (kind picker + body, with the mic — see T6).
- [ ] Verify: logging activity updates the going-cold indicator.

### T6 — Voice notes via dictation — spec §6 (port from AppTracker)
- [ ] Copy `useDictation` + `MicButton` (and the optional `/api/dictation/transcribe` Whisper route + env) from `h:\apptracker` into this repo; wire the mic into the activity-body textarea (and any note field). Web Speech default, editable transcript before save; on save, insert the `activities` row (kind `note`) + a `voice_notes` row with the transcript. Audio not retained (spec §6).
- [ ] `src/lib/env.ts`: add optional `OPENAI_API_KEY` (Whisper) — graceful no-op when unset (browser STT still works).
- [ ] Verify: dictate a note on a lead; transcript saved as the activity/voice_note.

### T7 — Pipeline dashboard — spec §4
- [ ] `src/lib/db/queries/dashboard.ts`: counts + total value of leads per status (funnel); active projects with overdue flag; revenue (invoiced vs paid this month + outstanding/overdue); recent activity feed (latest N across subjects).
- [ ] Replace `(app)/dashboard/page.tsx` with the CRM dashboard: pipeline board, revenue cards, active projects, activity feed; each count links to the filtered list (drill-down).
- [ ] Verify: the acceptance flow is reflected on the dashboard.

### T8 — Public lead-capture form — spec §3.1, §8
- [ ] `src/app/(public)/lead/page.tsx` (or `app/lead/page.tsx`) — the ONLY public route (not under the auth-gated `(app)` group; confirm `proxy.ts` allows it). Form → a public server action that inserts a lead (`source="web"`, owner = the OWNER_EMAIL user). Add a honeypot/basic rate guard.
- [ ] Verify: submit the public form while logged out → lead appears in the pipeline.

### T9 — Verify & acceptance (spec §9)
- [ ] `npm run typecheck` + `npm run lint` + `npm test` green.
- [ ] `npm run dev`; run the full acceptance flow end to end (lead→won→client→project→invoice→PDF→dictated note→dashboard) behind login.
- [ ] (Optional) `check-app-production-readiness`.
- [ ] Produce the built-vs-stubbed report. Out-of-scope stubs (spec §8): card payments, email drip, client portal, calendar sync, multi-currency, MFA, owner-configurable stages, server-side-only transcription.

## MVP cut line (confirm at execution)
**Core (build now):** T0–T9 above = the full accepted lifecycle. **Labeled stubs (don't build):** the
spec §8 out-of-scope list. If time-boxed, build T0–T2 + T5–T7 first (lead→client + activities +
dictation + dashboard) as the runnable spine; T3/T4/T8 next.
