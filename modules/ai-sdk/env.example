# Vercel AI Gateway API key (https://vercel.com/dashboard/ai-gateway → API keys).
# One key fronts every provider (Anthropic, OpenAI, Google, etc.) so the chat
# UI can switch models with no extra credentials. Must start with `vck_`.
AI_GATEWAY_API_KEY="vck_..."
